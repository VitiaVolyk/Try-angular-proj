import { Rout3Directive } from './rout3.directive';

describe('Rout3Directive', () => {
  it('should create an instance', () => {
    const directive = new Rout3Directive();
    expect(directive).toBeTruthy();
  });
});
